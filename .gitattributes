# Normalize line endings: LF in the repo, auto on checkout.
# Keeps Windows dev machines and Linux CI runners byte-consistent.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webm binary
*.zip binary
*.ico binary
